SELECT 
  c.name AS category_name,
  MAX(p.price) AS max_price
FROM 
  category_list c
JOIN 
  product_list p ON p.category_id = c.id
GROUP BY 
  c.name
HAVING 
  COUNT(p.id) >= 3
ORDER BY 
  max_price ASC;
